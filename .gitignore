.env
.env.*
!.env.example
.vercel
node_modules/
desktop.ini
Thumbs.db
ehthumbs.db
.env*.local
.claude/
.DS_Store

# BD research — private contact data lives beside public analysis but never
# gets committed. See bd-research/bni-swva-pro/README.md.
*.local.md
bd-research/**/CONTACTS_LOCAL.md

# Claude session work folders + private notes (not for production repo)
_claude/
_internal/

# Client-specific marketing repos that live HERE during dev but ship from
# their own repo. Never push to vtv-assessment.
gergacs-marketing-system/

# CLAUDE.md / context files — may contain credentials or PINs for AI sessions.
# Keep on disk for Claude Code to read locally; never commit to a public repo.
# Added 2026-05-03 after CLAUDE.md was found tracked with ADMIN_API_KEY + PIN.
CLAUDE.md
CLAUDE.local.md
.claude.md
